package com.ftd.smartshare.data.dao;

import com.ftd.smartshare.dto.DownloadRequestDto;

public class DownloadDao implements BaseDao {
	
	public DownloadRequestDto getFile(Reques dto) {
		
		select file array from filename
		re;
		
		//create download response dto
		//return file response dto, marshall and send back to client
		
		//on server object unmarshaller.unmarshall to an object
		//if(request.instance of uploadrequest)
		    //save to db
		else (request instance of download request){
			
			if password match send file reader out
		}
		
	}
	

}
